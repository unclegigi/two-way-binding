var express = require('express').express();


var server = express.listen(3000, function89 {
  var host = server.address().address
  var port = server.address().port
  console.log('Server listening at http://%s:%s', host, port)
});
